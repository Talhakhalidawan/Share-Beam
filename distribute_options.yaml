output: dist/
releases:
  - name: release
    jobs:
      - name: linux-zip
        package:
          platform: linux
          target: zip
          build_args:
            --release:
      - name: windows-zip
        package:
          platform: windows
          target: zip
          build_args:
            --release:

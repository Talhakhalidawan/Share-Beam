name: sharebeam
description: "Professional cross-platform file sharing application."
version: 1.0.0+1
homepage: https://github.com/talhakhalid/sharebeam
repository: https://github.com/talhakhalid/sharebeam

environment:
  sdk: ^3.4.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  
  super_clipboard: ^0.9.1
  super_native_extensions: ^0.9.1
  shared_preferences: ^2.5.3
  pasteboard: ^0.3.0
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  intl: ^0.18.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  web_socket_channel: ^3.0.3
  bonsoir: ^6.1.0
  file_picker: ^8.1.0
  path_provider: ^2.1.5
  share_plus: ^12.0.2
  shelf_web_socket: ^3.0.0
  device_info_plus: ^10.1.2
  file_selector: ^1.1.0
  permission_handler: ^12.0.1
  gal: ^2.3.2
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.14.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  msix: ^3.16.13

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/logo.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

msix_config:
  display_name: Sharebeam
  publisher_display_name: Talha Khalid
  publisher: CN=TalhaKhalid, O=TalhaKhalid, L=Lahore, S=Punjab, C=PK
  identity_name: com.talhakhalid.sharebeam
  msix_version: 1.0.0.0
  logo_path: assets/images/logo.png
  capabilities: internetClient, internetClientServer, privateNetworkClientServer
  install_certificate: false

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/images/logo.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/logo.png"
  web:
    generate: true
    image_path: "assets/images/logo.png"
  windows:
    generate: true
    image_path: "assets/images/logo.png"
  linux:
    generate: true
    image_path: "assets/images/logo.png"
  min_sdk_android: 21
